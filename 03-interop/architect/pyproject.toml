[project]
name = "architect-a2a"
version = "0.1.0"
description = "The remote Architect A2A agent (NVIDIA NIM), served over HTTP — its own deployable service."
requires-python = ">=3.10"
dependencies = ["fastapi>=0.115.0", "uvicorn>=0.30.0", "httpx>=0.27.0", "python-dotenv>=1.0.0"]
