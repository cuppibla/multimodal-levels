{
  "name": "level-01-beacon",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Way Back Home · Level 1 — constrained multimodal generation + keeping one identity consistent across a chat session.",
  "scripts": {
    "prompt": "tsx src/run.ts --prompt",
    "naive": "tsx src/run.ts --naive",
    "identity": "tsx src/run.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@google/genai": "^2.10.0",
    "dotenv": "^17.2.3"
  },
  "devDependencies": {
    "@types/node": "^20",
    "tsx": "^4.20.6",
    "typescript": "^5"
  }
}
