{
  "name": "level-02-perceive",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Multimodal Levels · Level 2 — how one specialist perceives: READ (multimodal) + MEASURE (embedding).",
  "scripts": {
    "samples": "tsx src/samples.ts",
    "perceive": "tsx src/run.ts",
    "roster": "tsx src/run.ts --roster",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@google/genai": "^2.10.0",
    "dotenv": "^17.2.3"
  },
  "devDependencies": {
    "@types/node": "^20",
    "tsx": "^4.20.6",
    "typescript": "^5"
  }
}
